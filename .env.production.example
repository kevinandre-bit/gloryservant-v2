APP_NAME=GloryServant
APP_ENV=production
# Generate and inject securely on server: php artisan key:generate --show
APP_KEY=
APP_DEBUG=false
APP_URL=https://gloryservant.com

LOG_CHANNEL=stack
LOG_LEVEL=error

# Database (rotate if previously committed)
DB_CONNECTION=mysql
DB_HOST=
DB_PORT=3306
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

# Cache/Queue/Sessions
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_DOMAIN=.gloryservant.com
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

# CORS — enumerate production origins only
CORS_ALLOWED_ORIGINS="https://gloryservant.com,https://admin.gloryservant.com"
# Set to true only if using cookie-based auth from a SPA
CORS_SUPPORTS_CREDENTIALS=false

# Sanctum (if applicable)
SANCTUM_STATEFUL_DOMAINS=gloryservant.com,admin.gloryservant.com

# Redis (if used)
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=
REDIS_PORT=6379

# Mail (rotate if previously committed)
MAIL_MAILER=smtp
MAIL_HOST=
MAIL_PORT=
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=no-reply@gloryservant.com
MAIL_FROM_NAME="GloryServant"

# AWS / third-party (rotate if previously committed)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1
